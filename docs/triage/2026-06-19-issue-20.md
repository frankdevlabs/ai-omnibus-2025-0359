# Triage: issue #20 — Source health (T1-01/02/03/07/08)

- **Issue:** https://github.com/frankdevlabs/ai-omnibus-2025-0359/issues/20
- **Source:** Weekly source-health log (`T1-01`, `T1-02`, `T1-03`, `T1-07`, `T1-08`)
- **Detected:** 2026-06-19
- **Type / playbook:** source-health / source-health

## What was flagged

The weekly source-health log logged one failing-run symptom on 2026-06-19:

- `T1-08` (EDPB–EDPS Joint Opinion 1/2026 page) returned an AWS-WAF JS challenge
  page (1983 B, `awsWafCookie`) to `curl` after retry — it was reachable
  2026-06-18. Hash frozen at last good capture; treated as transient.

The issue body also notes that `T1-03` (EUR-Lex procedure) and `T1-07`
(doceo A-10-2026-0073) "remain unreachable-by-design (AWS-WAF 202)" — expected,
**not** health failures — and that file status is tracked via `T1-01` (OEIL) and
`T1-02` (Cellar XML).

## Assessment

This is a fetch-health log, not a content change. Live re-check 2026-06-19 confirms
nothing is stale, moved, or defunct:

| Code | Live check | Verdict |
|---|---|---|
| `T1-01` OEIL | HTTP 200, 162 KB | **Healthy.** Primary watch; no action. |
| `T1-02` Cellar | HTTP 303 → 200, 217 KB RDF | **Healthy.** 303 is content-negotiation, not a failure; following the redirect returns the object. No action. |
| `T1-03` EUR-Lex procedure | HTTP 202 (AWS-WAF) | **Unreachable-by-design.** Already documented in the note (prefer Cellar `T1-02`). No action. |
| `T1-07` doceo report | HTTP 202 (AWS-WAF) | **Unreachable-by-design.** Operative text already committed (`sources/parliament/A10-0073-2026_imco-libe-report_2026-03-19.pdf`). Note lacks the WAF caveat → annotate so the routine stops treating it as a health hit. |
| `T1-08` EDPB–EDPS JO | HTTP 403 (AWS-WAF challenge) | **Transient block, no substance lost.** Opinion published 21 Jan 2026, committed (`sources/edpb-edps/EDPB-EDPS-JO-1-2026_joint-opinion_2026-01-20.pdf`) and digested (`docs/advisory/edpb-edps-jo-1-2026.md`); no new output expected. Note empty → annotate as published/transient. |

No genuine source-health failure: every code is healthy, blocked-by-design, or a
transient block on an already-captured, already-published document. No URL needs
correction or replacement.

## Plan

Minimal watchlist hygiene only — annotate the two notes that let the routine
re-flag by-design / transient blocks as health hits. No `documents.yaml` changes
(every document is already committed correctly); no URL changes.

1. `tracker.yaml` — `T1-07` note: add that doceo bot-blocks (202/AWS-WAF) to
   non-browser clients and that the operative text is already in hand
   (`A10-0073-2026`), so a 202 here is expected, not a failure.
2. `tracker.yaml` — `T1-08` note (currently empty): record that the opinion is
   published (21 Jan 2026) and committed/digested, and that periodic 403/AWS-WAF
   challenges are transient and non-blocking (substance already captured).

## Verification

- `python3 .claude/skills/transcribe-council-extract/linkcheck.py .` → "0 broken".
- Re-read `tracker.yaml` `T1-07`/`T1-08` notes; confirm the by-design/transient
  caveats are present.
- Close #20 once merged (no further integration owed).
